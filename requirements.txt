flask>=3.0
requests>=2.31
numpy>=1.24
Pillow>=10.0
groq>=0.5
gunicorn>=21.2
SpeechRecognition>=3.10
flask-limiter>=3.5
python-dotenv>=1.0
structlog>=23.0

# Optional: offline STT via faster-whisper (model tiny ~75 MB, base ~142 MB)
# Requires numpy (already listed). Install with: pip install faster-whisper[cuda]
# Set WHISPER_MODEL_SIZE=tiny|base|small|medium|large-v3
# faster-whisper
